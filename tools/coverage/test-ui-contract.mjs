import assert from 'node:assert/strict';
import fs from 'node:fs';

const html = fs.readFileSync(new URL('../../index.html', import.meta.url), 'utf8');
const required = [
  'id="vFullscreen"',
  'id="vThumbToggle"',
  'id="vInfoToggle"',
  'id="vResizeLeft"',
  'id="vResizeRight"',
  'id="boardListSearch"',
  'id="processingPosition"',
  'function recordRowDecision(',
  'function filenameBoardCandidatesOf(',
  'function openBoardInViewer(',
  'function boardApprovalLogHtml(',
  'id="bdApproveRemaining"',
  'Enhanced extraction',
  'Protection fields',
  'host.requestFullscreen',
  "document.addEventListener('fullscreenchange'",
  'function syncViewerLayout()',
  'const ANALYSIS_VERSION=29;',
  'id="processingCancelBtn"',
  'const AI_RECOVERY_BATCH_SIZE=3;',
  'const AI_RECOVERY_PAGE_TIMEOUT_MS=180000;',
  'const AI_RECOVERY_TOTAL_BUDGET_MS=360000;',
  "const AI_BG_ENDPOINT='/api/extract-background';",
  "const AI_STATUS_ENDPOINT='/api/extract-status';",
  'return s.result;',
  'Background extraction returned no structured result',
  'active (AI agent team + master audit)',
  'function applyAiAudit(',
  'perception-contract|${fileId}|${pageNo}|${reasonCode}',
  'A.perceptionContracts=A.perceptionContracts||[]',
  '(!auto||opts?.forceAiRecovery)',
  'noRecovery:true,noAi:true',
  '!opts.recoverLowConfidence',
  "(!state.cur.analysis||state.cur.analysis.version!==ANALYSIS_VERSION",
  'function recoveryCandidates(',
  'async function runAnalysisWithRecovery(',
  'core.parseSpatialSchematicPage(',
  'schematic-topology-core.js',
  'function readPdfVectorGeometry(',
  'async function renderSourcePageCanvas(',
  'validateRasterTracePath?.({',
  "crossingPolicy:'explicit_visual_trace_with_raster_continuity'",
  'annotationMode:lib.AnnotationMode?.DISABLE??0',
  'vectorGeometry:pg.vectorGeometry||null',
  "sourceRole:topologyOnly?'schematic_ai_feeder':'ai_feeder'",
  'function appendSchematicPaths(',
  "detection.kind==='feeder'",
  "String(feed.sourceRole||'').startsWith('schematic')",
  "&&!schematicSpatial?.matched",
  "const governingNotesByFile=new Map();",
  "strategy:'geometry-prior-schema'",
  "strategy:'geometry-single-way'",
  "core.parseSpatialScheduleDocument(candidates",
  'spatialSchemaSourcePage:',
  'filenameBoardCandidates.length===1',
  "filename_reference_review",
  "filename_board_reference_used_for_review",
  'data-report-mode="board"',
  'data-action="edit"',
  "openRowEditor(d.r,false,'Viewer')",
  "bbox:r.highlightBbox||r.bbox",
  "'RCD Protection':evidence",
  "'RCD Arrangement':evidence",
  'id="eRcdArrangement"',
  'function rowApprovalIssue(row)',
  'function currentReportExportReadiness(',
  'buildReportExportReadiness({health:',
  'Audit complete — export permitted with',
  'takeoffEvidence:',
  'spatialBlockingReasons:',
  'id="guidedReviewBtn"',
  'id="vReviewBar"',
  'function orderedPendingReviewRows()',
  'function reviewBoardSourceTuple(',
  'function guidedReviewBoardKeys(',
  'function advanceGuidedReview(',
  'function restoreGuidedReviewSession(',
  'window.EstimationReviewCore.restoreSession(',
  'await persistProjectNow(state.cur)',
  'const sameBoard=queue.filter(',
  'function viewerBoardsOnPage(',
  'function viewerBoardOnPage(',
  'function syncViewerBoardToPage(',
  'syncViewerBoardToPage(f,V.page);',
  'Start audit',
  'Board ${Math.min(boardOrder.length,boardPosition+1)} of ${boardOrder.length}',
  'function openReportSources(',
  'function openReportBoardSources(',
  'data-report-source-key',
  'data-report-board-source',
  'function placeModalInActiveSurface()',
  "classList.add('row-editor-modal','is-flexible')",
  'id="eRole"',
  'id="eBoardNew"',
  'id="eCorrectionReason"',
  "openBoardEditor(d.scheduleNorm,'Schematic cross-reference')",
  "surface:'Schematic cross-reference'",
  'function deviceSpecificationKey(row)',
  'function viewerSpecificationCounts(row,key,rows)',
  'function viewerDetectionCardHtml(d,index)',
  'function bindViewerDocumentRows(detections)',
  'function syncViewerDetectionList(rowId',
  'Extraction incomplete',
  'No schedule rows were captured',
  'function openMissingWaysEditor(gap)',
  'id="gapRecoverWays"',
  'id="gapAddRow"',
  'id="gapCorrectBoard"',
  'data-row-id=',
  'reconcileCombinedProtection(row)',
  'schematicDevices:[]',
  'function isTakeoffEvidenceRow(row)',
  "!['mention','ai'].includes(row.kind)||row.status==='confirmed'",
  'id="vCalibrateMode"',
  'id="vCalibrationRole"',
  'id="vCalibrationScope"',
  'function calibrationHintForPage(',
  'function calibrationRoleGroups(',
  'function bindCalibrationDrag(',
  'function applyCalibrations(',
  'calibrationHint.regions.length>0',
  'forceAiRecovery:true',
  'function primaryBoardForPage(',
  'ai_board_ref_conflicts_with_primary',
  'deterministic_primary_board:',
  'function viewerAuditTakeoffSummary(',
  'approved take-off',
  'function queueAnalysisAuditPrompt(',
  "'Open audit','Stay here'",
  "const BOARD_SCHEDULE_TYPES=new Set(['db-schedule','main-schedule','equipment-schedule']);",
  '||BOARD_SCHEDULE_TYPES.has(page.type)',
  'const schedulePage=BOARD_SCHEDULE_TYPES.has(pg.type)',
  "const TRIP_UNIT_OPTIONS=window.EstimationReport?.TRIP_UNIT_OPTIONS||['LSI','LSIG','LSNI','TM','ATFM','ATAM','LI'];",
  'id="eProductRange"',
];
required.forEach((value) => assert.ok(html.includes(value), `missing UI contract: ${value}`));
assert.ok(!html.includes("automaticScope?.enforced&&!automaticScope.pageSet.has(pageNo)"),
  'content-derived scope must never exclude pages from deterministic or enhanced analysis');
assert.ok(!html.includes("pg.type!=='cable-schedule'"),
  'cable schedules with protective-device columns must enter spatial schedule analysis');
assert.match(html, /trimbleCableScheduleProfile\(spatialWords,pg\.w\)\.matched/,
  'structurally proven cable schedules must bypass a wrong page-type label');
assert.ok(!html.includes('id="vAssistBoard"'), 'viewer must not contain a duplicate board selector');
assert.ok(!/\bSpace way(?:s)?\b/i.test(html), 'user-facing empty positions must be labelled as fitted blanks, not Space ways');
assert.ok(html.includes('Fitted blank way'), 'viewer must use the commercial fitted-blank label');
assert.ok(!html.includes('Match rows'), 'ambiguous Match rows command must not return');
assert.ok(!html.includes('Approve checked rows'), 'bulk approval must not claim rows are checked');
assert.ok(!html.includes("slice(0,90)"), 'viewer source evidence must not be silently truncated');
assert.ok(!html.includes('setTimeout(()=>controller.abort(),240000)'), 'enhanced extraction must not retain a four-minute per-page wait');
assert.ok(!/kind:'schematic'[\s\S]{0,500}A\.rows\.push/.test(html), 'schematic topology must not be inserted into take-off rows');

const markup = html.slice(0, html.indexOf('<script'));
const ids = [...markup.matchAll(/\bid="([^"]+)"/g)].map((match) => match[1]);
const duplicates = ids.filter((id, index) => ids.indexOf(id) !== index);
assert.deepEqual([...new Set(duplicates)], [], `duplicate DOM ids: ${duplicates.join(', ')}`);

const inline = html.match(/<script>([\s\S]*?)<\/script>/)?.[1];
assert.ok(inline, 'inline application script not found');
new Function(inline);

console.log('PASS: analysis, guided review, report-source, approval-log, and viewer UI contracts');
